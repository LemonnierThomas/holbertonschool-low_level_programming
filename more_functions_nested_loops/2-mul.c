#include "main.h"

/**
 * mul - code that will multiply two integers.
 * @a: one of the value that will be multiplied.
 * @b: the second one.
 * result: is the result of the multiplication.
 * Return: Return the value.
 */

int mul(int a, int b)
{
	int result = a * b;

return (result);
}
